<%= form_for [@project, @comment] do |f| %>

  <div class="field">
    <%= f.label :comment, name: 'Comment' %><br>
    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit "Valider"%>
  </div>
<% end %>
