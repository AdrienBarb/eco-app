<h1><%= @project.name %></h1>
<p><%= @project.description %></p>
<p><%= @project.category.category %></p>

<%= link_to 'Modifier mon projet', edit_user_project_path(@user, @project) %>
<%= link_to "Suprimer", user_project_path(@user, @project), method: :delete,
                                                            data: { confirm: "Are you sure you want to delete this ticket?"},
                                                            class: "delete" %>
